pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                bat 'npm run dev'
                bat 'start http://localhost:5173/'
            }
        }
    }
}
