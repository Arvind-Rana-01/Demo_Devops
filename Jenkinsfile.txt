pipeline{
    agent any   // don't have any ec2 instance so run it in any
    stages{
        stage('build'){
            steps{
                bat 'echo Dev branch of version v1.2'
		bat 'cd react-app'
		bat 'npm install'
		bat 'npm run dev'
		bat 'start http://localhost:5173/'
            }
        }
    }
}