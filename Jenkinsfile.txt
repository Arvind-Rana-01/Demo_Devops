pipeline {
    agent any

    scm {
        git {
            url 'https://github.com/Arvind-Rana-01/Demo_Devops.git'
            credentialsId 'my-credentials'
            branch '*/main'
        }
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
            }
        }
    }
}
